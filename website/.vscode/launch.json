{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Website",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "command": "npx prisma db push && npm run dev"
    },
  ]
}